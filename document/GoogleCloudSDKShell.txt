# gcloudの認証・ログイン
gcloud auth login

# 利用するGCPプロジェクトのセット
gcloud config set project zekisoft-340512

# 設定（GCPプロジェクト）を確認
gcloud config list

# Dockerリポジトリの認証（初回のみ）
gcloud auth configure-docker

# Cloud BuildでビルドしてContainer Registryにプッシュ
cd C:\Users\hatoi\Documents\python-typing-tube
gcloud builds submit --region us-central1 --config cloudbuild.yaml

# Clourd Runへデプロイは手動実行
https://console.cloud.google.com/gcr/images/zekisoft-340512/global/python-typing-tube?project=zekisoft-340512
